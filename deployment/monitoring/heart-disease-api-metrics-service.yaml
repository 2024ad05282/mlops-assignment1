apiVersion: v1
kind: Service
metadata:
  name: heart-disease-api-metrics
  labels:
    app: heart-disease-api
spec:
  type: ClusterIP
  selector:
    app: heart-disease-api
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      name: metrics
